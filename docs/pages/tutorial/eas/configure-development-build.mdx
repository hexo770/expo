import React from "react";
import { View, Text, StyleSheet, TouchableOpacity } from "react-native";
import { MaterialIcons, FontAwesome5, Entypo, Ionicons } from '@expo/vector-icons';

export default function HomeScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>تطبيق ديني</Text>
      <View style={styles.grid}>
        {/* الأذكار */}
        <TouchableOpacity
          style={styles.circleButton}
          onPress={() => navigation.navigate("Azkar")}
        >
          <FontAwesome5 name="praying-hands" size={50} color="#fff" />
          <Text style={styles.label}>الأذكار</Text>
        </TouchableOpacity>

        {/* الأحاديث */}
        <TouchableOpacity
          style={styles.circleButton}
          onPress={() => navigation.navigate("Ahadith")}
        >
          <Entypo name="book" size={50} color="#fff" />
          <Text style={styles.label}>الأحاديث</Text>
        </TouchableOpacity>

        {/* التحفيز */}
        <TouchableOpacity
          style={styles.circleButton}
          onPress={() => navigation.navigate("Motivation")}
        >
          <Ionicons name="bulb" size={50} color="#fff" />
          <Text style={styles.label}>التحفيز</Text>
        </TouchableOpacity>

        {/* الإعدادات */}
        <TouchableOpacity
          style={styles.circleButton}
          onPress={() => navigation.navigate("Settings")}
        >
          <MaterialIcons name="settings" size={50} color="#fff" />
          <Text style={styles.label}>الإعدادات</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fdf6e3", // خلفية هادئة
    alignItems: "center",
    paddingTop: 60,
  },
  title: {
    fontSize: 28,
    fontWeight: "bold",
    marginBottom: 40,
  },
  grid: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-around",
    width: "100%",
  },
  circleButton: {
    backgroundColor: "#4caf50", // لون أيقونة أخضر هادئ
    width: 120,
    height: 120,
    borderRadius: 60,
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 30,
  },
  label: {
    marginTop: 10,
    fontSize: 16,
    color: "#fff",
    fontWeight: "bold",
  },
});
